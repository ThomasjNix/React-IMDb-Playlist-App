import React from 'react'

const SingleResult = () => {
    return (
        <div>
            {/* TODO - title, button to add (select playlist?) button to remove if in playlist */}
        </div>
    )
}

export default SingleResult
