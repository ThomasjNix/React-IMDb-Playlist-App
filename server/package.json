{
  "name": "server",
  "version": "1.0.0",
  "description": "Server for OMDBAPI App",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-ui": "cd ../imdb-playlist-app && npm run build",
    "start-watch": "npm run build-ui && node server.js"
  },
  "author": "Thomas Nix",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.15"
  }
}
